#include"libft.h"
#include<stdio.h>
#include<string.h>

int main()
/* {
	char haystack[30] = "aaabcabcd";
	//char needle[10] = "aabc";
	//size_t max = strlen(s2);

	printf("%s\n", strnstr(haystack, "a", -1) );
	printf("%s\n", ft_strnstr(haystack, "a", -1));
	//printf("%d\n", ft_memcmp(s1, s2, n));
	return 0;
} */
{
	char *s = "jacu";

	ft_putstr_fd(s, 1);
}